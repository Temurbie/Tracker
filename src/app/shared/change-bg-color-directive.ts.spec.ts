import { ChangeBgColorDirectiveTs } from './change-bg-color-directive.ts';

describe('ChangeBgColorDirectiveTs', () => {
  it('should create an instance', () => {
    const directive = new ChangeBgColorDirectiveTs();
    expect(directive).toBeTruthy();
  });
});
